# SafeRoute Client Environment Configuration Template
# For local development, copy this file to .env or .env.local

# Optional URL of the SafeRoute Backend API (no trailing slash).
# Leave unset when Express serves the built frontend on port 5000; requests
# will use relative /api/... URLs. Set this only when running Vite separately.
VITE_API_BASE_URL=
